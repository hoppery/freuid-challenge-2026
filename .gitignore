# Data (never commit — large + licensed non-commercial). Anchored to repo root
# so it does NOT match source dirs like src/freuid/data.
/data/
manifests/*.parquet
manifests/*.csv
*.zip
*.tar
*.tar.gz

# Secrets — never commit credentials
**/kaggle.json
**/access_token
.env
*.pem

# Model artifacts
checkpoints/
runs/
wandb/
*.ckpt
*.pt
*.pth
lightning_logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# --- submission-repo additions (experiment outputs / large registries not needed to reproduce) ---
keep/
.reports/
reports/
.capdumps/
/submission*.csv
/*.csv
docs/presentation/
docs/superpowers/
.claude/
_transfer/checkpoints/
_transfer/.reports/
_transfer/wandb/

# LaTeX build artifacts (technical report) — keep the .tex/.pdf/.bib, drop the build junk
*.aux
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
docs/technical_report.log
docs/technical_report.out
