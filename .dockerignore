# Keep the build context small. Weights are NOT in the context anymore — the Dockerfile fetches the
# 3 capture checkpoints from the repo's GitHub Release at build time (see Dockerfile WEIGHTS_BASE).
checkpoints/
data/
_transfer/
manifests/
reports/
.reports/
.capdumps/
.git/
docs/
tests/
submission_*.csv
*.csv
.ruff_cache/
.pytest_cache/
__pycache__/
**/__pycache__/
*.pyc
