# E5 (2026-06-18) CAPTURE max-diversity captured-genuine: E2 base + MIDV-500 (18 types) + MIDV-Holo genuine, ×2 upweight, NO fraud. Targets E4g diagnosed weakness (narrow domain). Goal: beat 0.5466.
# data (E2: chance 0.95 → 0.5466). Architecture/loss are exhausted (E3 FHAG/focal/CDC + TENT all
# failed). E4 adds the untapped MIDV-Holo CAPTURED data to the E2-early recipe: 790 real
# smartphone-captured genuine holographic passports (×4 upweight) + 1200 real captured forgeries
# (photo_replacement / holo_copy / copy_without_holo / pseudo_holo) — the FIRST real captured ATTACKS
# in training (E2 had only GenAI attacks). Goal: beat E2-early fantasyid_test proxy 0.5466. Pick best epoch.
manifest: manifests/freuid_fid_capgen4.parquet
backbone: vit_base_patch14_reg4_dinov2.lvd142m
model_type: dtc
img_size: 896
batch_size: 24
epochs: 6
lr: 0.0001
weight_decay: 0.05
num_workers: 14
val_fraction: 0.1
holdout_by: ""
seed: 42
out_dir: checkpoints/exp_e5_capgen4_ep6
amp: true
pretrained: true
freeze_backbone: true
unfreeze_blocks: 2
dtc_lambda: 0.5
tracemix_p: 0.5
heavy_recapture: true
save_every_epoch: true
