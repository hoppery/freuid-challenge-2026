# prod896 replica (user's other-PC line: DINOv2-uf2 + FDA @896 production, all-5-types, public
# 0.0798). This session's exp_deploy896_all (no FDA) only got public 0.2159 → the key missing
# ingredient is FDA (cross-type amplitude swap, validated stabilizer). Strong base to apply the
# validated TENT lever (+43% LODO) + 728/896 resolution ensemble. Aggressive GPU (batch 32 +
# workers 28; FDA adds a donor image read so slightly lower batch than 48).
manifest: manifests/freuid.parquet
backbone: vit_base_patch14_reg4_dinov2.lvd142m
model_type: dtc
img_size: 896
batch_size: 32
epochs: 6
lr: 0.0001
weight_decay: 0.05
num_workers: 28
val_fraction: 0.12
holdout_by: ""
seed: 42
out_dir: checkpoints/exp_prod896_fda_all
amp: true
pretrained: true
freeze_backbone: true
unfreeze_blocks: 2
dtc_lambda: 0.5
tracemix_p: 0.5
fda_p: 0.5
