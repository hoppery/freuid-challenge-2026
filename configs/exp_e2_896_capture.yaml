# E2-896: CAPTURE/PHYSICAL robustness (host's #1 prize axis — private suppresses fragile digital
# artifacts via physical capture). Train on FREUID + REAL physical data (FantasyID-physical
# upweighted) + heavy print-and-capture aug. The ONLY lever that moved the capture proxy (E2-518:
# FantasyID 0.586->0.718). Validate on leakage-free fantasyid_test (1,295). Capture-robust RESERVE
# (external data has LODO tension; primary unseen-type model stays FREUID-only 896+TENT).
manifest: manifests/freuid_fid_upweighted.parquet
backbone: vit_base_patch14_reg4_dinov2.lvd142m
model_type: dtc
img_size: 896
batch_size: 24
epochs: 4
lr: 0.0001
weight_decay: 0.05
num_workers: 28
val_fraction: 0.1
holdout_by: ""
seed: 42
out_dir: checkpoints/exp_e2_896_capture
amp: true
pretrained: true
freeze_backbone: true
unfreeze_blocks: 2
dtc_lambda: 0.5
tracemix_p: 0.5
heavy_recapture: true
