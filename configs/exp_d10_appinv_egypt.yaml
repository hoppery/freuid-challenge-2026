# D10: appearance-invariance experiment. 896px EGYPT LODO + strong color/palette
# randomization. Baseline to beat: 896 EGYPT 0.0217. batch 8 (clean vs 896 EGYPT).
manifest: manifests/freuid.parquet
backbone: vit_base_patch14_reg4_dinov2.lvd142m
model_type: dtc
img_size: 896
batch_size: 8
epochs: 8
lr: 0.0001
weight_decay: 0.05
num_workers: 16
val_fraction: 0.15
holdout_by: doc_type
holdout_groups: "EGYPT/DL"
seed: 42
out_dir: checkpoints/exp_d10_appinv_egypt
amp: true
pretrained: true
freeze_backbone: true
unfreeze_blocks: 2
dtc_lambda: 0.5
tracemix_p: 0.5
appearance_inv: true
